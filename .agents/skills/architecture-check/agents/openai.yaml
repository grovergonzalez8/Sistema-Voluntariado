interface:
  display_name: 'Architecture Check'
  short_description: 'Review module boundaries and dependencies'
  default_prompt: 'Use $architecture-check to review this change for Clean Architecture boundary violations.'
