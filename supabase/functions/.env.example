# Copy this template to .env.local before serving Functions locally.
# Keep the values exact: do not use wildcards and do not add secrets here.
APP_ORIGIN=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173
